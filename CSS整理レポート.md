# CSS整理レポート

## 結果

- 対象: HTML 12ファイル、JavaScript 28ファイル、CSS 1ファイル
- CSS: 262,011 bytes → 223,519 bytes（38,492 bytes／約14.7%削減）
- 行数: 18,398行 → 16,025行（2,373行削減）
- 削除した未使用ルール: 337件
- セレクタリストから除外した未使用セレクタを含むルール: 10件
- 除外したセレクタ記述: 合計393件

## 判定方法

全HTMLと全JavaScriptを横断し、HTMLに直接書かれたクラス・IDだけでなく、JavaScriptのテンプレート文字列、`className`、`classList`などで使われる名前も照合しました。CSSセレクタ中のクラスまたはIDがプロジェクト内のどこにも存在しない場合だけ、未使用として削除しています。

`:is()`、`:where()`、`:not()`、`:has()`、エスケープを含む複雑なセレクタは、安全のため自動削除していません。メディアクエリなど条件付きルールも維持しています。

## 検証

- 整理後CSSのブロック構造を検証済み
- 全JavaScriptを `node --check` で構文検証済み
- HTML・JavaScript側のファイル内容は変更していません
- 元のプロジェクトは変更せず、整理版を別フォルダに作成しています

なお、元ZIPの `event-detail.html` は `event-like.js` を読み込む指定がありますが、そのファイル自体は元ZIPに含まれていません。今回のCSS整理とは無関係な既存の不足なので、自動生成や参照削除は行っていません。

## 補足

今回の整理は静的な参照照合に基づきます。外部スクリプト、CMS、ブラウザ拡張などが実行時に新しいクラス名を組み立てて追加する場合、その外部情報は判定対象外です。このプロジェクト内のHTML・JavaScriptだけで動作する前提の整理版です。

## 追加修正

- BLOG記事とGUIDE記事のフッター直前にある「一覧へ戻る」リンクを、通常時から明確に読める薄背景・濃色・太字へ変更
- 訪問済みリンクでも文字色を維持
- キーボード操作時のフォーカス表示をホバー表示と統一
